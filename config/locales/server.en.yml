en:
  site_settings:
    process_manager_enabled: "Enable Process Manager"
    process_manager_overdue_days_default: "Default overdue threshold in days for process topics. Use 0 to disable overdue behavior by default."
    process_manager_charts_allowed_groups: "Groups allowed to view process burn-down charts in addition to admins."
  process_manager:
    topic_transition_action_description: 'Moved from "%{starting_process_step_name}" to "%{ending_process_step_name}" with action: "%{process_step_option_name}" by @%{username}'
    errors:
      no_midway_error: "You cannot create a topic midway through a process and this category is part of a process."
      transition_failed_stale_state_refreshing: "Transition Failed: probably due to stale UI state - please try again after refresh - refreshing!"
      charts_access_denied: "You are not allowed to view process charts."
      name_contains_non_ascii_chars: "Name must contain ASCII characters only"
    options:
      start: "Start"
      ready: "Ready"
  activerecord:
    errors:
      models:
        process_manager/process:
          attributes:
            name:
              blank: "A process must have a name"
        process_manager/process_step:
          attributes:
            name:
              blank: "A process step must have a name"
            category_id:
              blank: "You must select a Category"
